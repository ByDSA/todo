import Model, { CreationalModel, ID, Keys } from "./label.model";
import { assertIsCreationalModel, assertIsModel } from "./utils";

export default Model;

export {
  CreationalModel, ID, Keys, assertIsCreationalModel, assertIsModel,
};
